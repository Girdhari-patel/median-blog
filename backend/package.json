{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@grptl/median-common": "^1.0.0",
    "@prisma/client": "^6.17.1",
    "@prisma/extension-accelerate": "^2.0.2",
    "hono": "^4.10.1",
    "prisma": "^6.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
